# Assets

Place repository images in this directory.

The README expects the welcome image at:

```text
assets/welcome-image.svg
```

No image file was present in the working tree when this housekeeping pass ran.
If an image was added outside this checkout, move or copy it here and update the
README path if the extension changes.

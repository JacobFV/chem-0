{
  "name": "@chem0/electron",
  "productName": "Chem-0 Lab Console",
  "description": "Local LeRobot experiment console backed by chem-0.",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-renderer.mjs",
    "dev": "npm run build && electron dist/main/main.js"
  },
  "dependencies": {
    "@chem0/backend": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "electron": "^33.2.1",
    "typescript": "^5.7.2"
  }
}
